<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rodrigobresan.samplearchitcturecomponents">

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".lifecycle.LifecycleSampleActivity" />
        <activity android:name=".viewmodel.counter.CounterActivity" />
        <activity android:name=".viewmodel.news.NewsActivity" />
        <activity android:name=".android_viewmodel.WifiListActivity" />

    </application>
</manifest>
